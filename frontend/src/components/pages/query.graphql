query User($id: ID! = "youya66"){
  user(id: $id){
    id
    name
    profileImage
    description
    articles{
      pageInfo{
        hasNextPage
      }
      edges{
        node{
          id
          url
          # stockCounts
          title
        }
      }
    }
  }
}